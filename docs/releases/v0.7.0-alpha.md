**Linier ERP v0.7.0-alpha — Purchase, Inventory & AP Accounting + Frontend Operations（2026-08-18 Release Candidate）**

### Release Summary

Sprint 5（5A Purchase Requisition/PO、5B Goods Receipt & Inbound、5C-1 Supplier Invoice / 3-Way Match / GRIR / AP）与 Sprint 6（6A Inventory Ledger、6B Inventory Operations）后端事实链全部 FINAL，叠加 Track A Frontend Operations（10 模块 List Workspace → F2-6B 动作接线 → B2-2A/B2-2B 项目财务/进度工作台 → Project Lifecycle L0-L2B1），使已 FINAL 的后端能力成为可操作工作台，并完成采购 → 入库 → 暂估应付 → 三单匹配 → 应付挂账（GRIR CONSUME + AP Liability + AP Open Item）的会计事实闭环。

### Release Gate

- 候选条件达成（CTO Directive 2026-08-12 §21）：5C-1 FINAL（PR #23 合并 main `5a8dcae`，CTO #9048/#9238/#9678 系列 FINAL）+ Frontend Operations Iteration 1 可用基线（B2-2A/B2-2B **31/31 Runtime Acceptance PASS — ACCEPTED**，docs/qa/B2-2_Runtime_QA.md）
- 发布基线 commit `990bf36`：CI 全绿（Quality Gates / Build / Secret Scanning，run #448）；生产已部署且 /api/health version=v0.7.0-alpha、/api/health/ready baseline=`0028_grir_historical_fact_backfill`
- 验收证据：docs/reviews/ReleaseGate_v0.7.0_Acceptance.md

### What's New

- **采购履约全链**：请购 → 审批 → 采购订单（APPROVED ≠ CONFIRMED，CONFIRMED 才可收货）→ 到货收货 → 质检（SKIP/SPOT/FULL）→ 入库（Created ≠ Posted，Posted 生效）→ 退货（来源可退额度 + 锁内防超退 + REPLACE_REQUIRED 重开 PO 履约）
- **库存账本（SSOT）**：入库/退货/调拨/盘点/调整/转换全部经 Shared InventoryLedgerCommand 原子落账；库存流水不可变、五元幂等、禁负库存、可追溯
- **供应商发票与应付**：SINV 创建即取号 → 三单匹配（immutable MatchRun + Workflow Approval Reference）→ 过账（POST）同事务产生 GRIR CONSUME + AP Liability + AP Open Item；金额 Server-side Decimal canonical；并发锁序 collect→dedupe→sort→FOR UPDATE
- **Frontend 工作台**：10 采购/库存模块 List+Detail+Create/Edit + Sales O2C 只读与 source-driven actions + CRM/Project Workspace（机会/项目/子资源/预算/费用/进度）+ Dashboard v2（四区模型）+ Supplier Invoice 前端（创建/匹配/过账）+ audit-logs 页面
- **Project Lifecycle**：验收（Acceptance）/ 结项（Close，stage authoritative，closure 不可删）/ 附件（Attachment contract）/ 阶段流转（Transition + allowedTransitions Read Contract）

### Compatibility / Migration / Upgrade

- Schema baseline：Migration 0001–0028（0027 = FROZEN BASELINE 禁改；0028 = GRIR Historical Backfill FINAL 冻结）
- Database：PostgreSQL 16（依赖 UNIQUE NULLS NOT DISTINCT、FOR UPDATE SKIP LOCKED）
- Breaking Changes：无（0001–0028 全部增量迁移）；GRIR / AP / MatchRun 不可变会计事实（禁止 UPDATE）；InventoryMovement COMMITTED 后不可变（纠错只能追加 Reversal/Correction）
- Upgrade：v0.6.0-alpha → 直接部署（0027/0028 自动应用；0028 幂等 backfill）；/api/health/ready baseline=true 即验证成功

### Known Limitations（2026-08-18）

1. **5C-2 HOLD**：供应商付款、AP 核销、付款冲销、供应商 CN/DN、AP 坏账核销、GL 过账未实现（前端不伪造权限入口）
2. **Reservation / AvailableQty HOLD**（Stock Projection 仅表达已 FINAL quantity fact）
3. **Inventory Costing HOLD**（FIFO / 移动平均 / 库存价值 / unitCost）
4. **Inventory Read Model Query API 未发布**（Query Contract 设计先行、实现 HOLD；前端库存余额/流水页为 Placeholder）
5. **Sprint 7 财务（GL / 总账 / 利润 / 现金流）HOLD**；BI / OA / Mobile 未开始
6. **Contract（合同）模块未开始**；Notification 真实发送后续
7. **Project Lifecycle Contract Audit 待收口**（FINAL/GAP/HOLD matrix = 下一 Governance 项）
8. **版本显示**：Footer 与 /api/health 显示 RELEASE_VERSION manifest（v0.7.0-alpha）

### HOLD（解除需 CTO 单独指令）

5C-2（Supplier Payment / AP Allocation / Supplier CN-DN / AP Write-Off / GL Posting）、Reservation、AvailableQty、FIFO、Moving Average、Inventory Costing、General Ledger、Financial Statements、BI、OA、Mobile、Inventory Read Model 实现。

### 变更详情

完整变更见 docs/CHANGELOG.md（[Unreleased]：5C-1 / F2-6 / B2-2 / L 系列 / P0 R1-R3）与 docs/RELEASE_NOTES.md（v0.7.0-alpha 段）。
